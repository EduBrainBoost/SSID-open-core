version: "2.0"
generated_utc: "2026-03-28T00:00:00Z"
deprecated: false
classification: "PUBLIC - Structure Exception Registry"
description: >
  Registers all authorized structural exceptions that deviate from the strict
  ROOT-24-LOCK policy. Every exception must carry a justification, an
  authorization source, and a date. Permanent exceptions require L0 approval.
  This file is the single source of truth for structure exceptions.

# ──────────────────────────────────────────────────────────
# PERMANENT EXCEPTIONS (L0 approved)
# ──────────────────────────────────────────────────────────
permanent_exceptions:
  - path: ".claude/"
    type: "directory"
    reason: >
      Claude Code configuration directory. Required for AI-assisted development
      tooling, prompt rules, and project-specific agent configuration. Contains
      rules/, projects/, and settings.json. Standard tooling convention.
    authorized_by: "L0"
    date: "2026-03-28"
    review_cycle: "annual"

  - path: "conftest.py"
    type: "file"
    reason: >
      Pytest shared fixture configuration. Must reside at repository root to
      provide cross-root test fixtures, path helpers, and session-scoped setup
      to all test suites under 11_test_simulation/. Required by pytest discovery.
    authorized_by: "L0"
    date: "2026-03-28"
    review_cycle: "annual"

  - path: "__pycache__/"
    type: "directory"
    reason: >
      Python bytecode cache directory. Automatically generated by the Python
      interpreter. Listed in .gitignore. Transient artifact that cannot be
      prevented during local development or CI runs.
    authorized_by: "auto"
    date: "2026-02-11"
    review_cycle: "none"

  - path: "pyproject.toml"
    type: "file"
    reason: >
      Python project configuration file. Required at repository root for
      tool configuration (pytest, black, ruff, mypy), dependency management,
      and build system specification per PEP 518/PEP 621. Standard Python
      ecosystem convention.
    authorized_by: "L0"
    date: "2026-03-28"
    review_cycle: "annual"

  - path: "docs/"
    type: "directory"
    reason: "Legacy documentation directory retained for backward-compatible links"
    authorized_by: "L0"
    date: "2026-02-11"
    review_cycle: "annual"

  - path: ".github/"
    type: "directory"
    reason: "GitHub-required CI/CD and repository configuration (workflows, issue templates, CODEOWNERS)"
    authorized_by: "L0"
    date: "2026-02-11"
    review_cycle: "annual"

# ──────────────────────────────────────────────────────────
# TEMPORARY EXCEPTIONS
# ──────────────────────────────────────────────────────────
temporary_exceptions: []

# ──────────────────────────────────────────────────────────
# GOVERNANCE RULES
# ──────────────────────────────────────────────────────────
rules:
  - "No exception may bypass ROOT-24-LOCK root count (must remain exactly 24)"
  - "Exceptions apply only to root-level items, not to root directory names"
  - "Every temporary exception must have an expiry_date"
  - "Expired exceptions are auto-revoked by CI gate"
  - "New permanent exceptions require RFC via 07_governance_legal"
  - "All exceptions are audited during quarterly compliance review"
  - "Removal of an exception requires the same approval level as its creation"

task_id: POC_TESTS_TST_001
title: "Extend edge-case tests for spec validation and forbidden paths"
task_type: tests
scope_allowlist:
  - "11_test_simulation/"
forbidden_paths:
  - "./"
  - "/mnt/data"
  - "**/.git/**"
  - "**/secrets/**"
required_checks:
  - "python 12_tooling/cli/stability_gate.py --run"
  - "python 12_tooling/cli/sot_validator.py --verify-all"
  - "python -m pytest -q"
acceptance_criteria:
  - "Edge-case tests for TaskSpec validation added"
  - "Forbidden paths enforcement tests added"
  - "Deterministic evidence serialization tests added"
  - "All tests pass including new ones"
evidence_outputs:
  - "02_audit_logging/agent_runs/POC_TESTS_TST_001/evidence.json"
  - "02_audit_logging/agent_runs/POC_TESTS_TST_001/patch.diff"
agent_cmd: "manual"
notes: "Test-only changes. No production code modified."

task_id: PH2_LOOP_REF_001
title: "Deterministic JSON output formatting for automation_loop (pure refactor)"
task_type: tooling
scope_allowlist:
  - "12_tooling/cli/automation_loop.py"
forbidden_paths:
  - "./"
  - "/mnt/data"
  - "**/.git/**"
  - "**/secrets/**"
required_checks:
  - "python 12_tooling/cli/stability_gate.py --run"
  - "python 12_tooling/cli/sot_validator.py --verify-all"
  - "python -m pytest -q"
acceptance_criteria:
  - "All JSON outputs use sorted keys (deterministic serialization)"
  - "Exit codes are clearly defined and documented in code"
  - "No semantic change to gate execution order"
  - "All existing tests pass without modification"
evidence_outputs:
  - "02_audit_logging/agent_runs/PH2_LOOP_REF_001/evidence.json"
  - "02_audit_logging/agent_runs/PH2_LOOP_REF_001/patch.diff"
agent_cmd: "manual"
notes: "Pure refactor only. Deterministic output enables diff-based CI checks. No new features, no scope_allowlist expansion."

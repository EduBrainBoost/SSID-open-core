{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://ssid.internal/schemas/sot_finding_schema.json",
  "title": "SoT Convergence Findings",
  "description": "Schema for standardized SoT convergence findings produced by scanners, validators, and policy engines. Each report captures all findings from a single convergence run between a canonical and derivative repository.",
  "type": "object",
  "required": [
    "version",
    "generated_at",
    "run_id",
    "findings",
    "summary"
  ],
  "additionalProperties": false,
  "properties": {
    "version": {
      "type": "string",
      "const": "1.0.0",
      "description": "Schema version of this findings report."
    },
    "generated_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 UTC timestamp when this findings report was generated."
    },
    "run_id": {
      "type": "string",
      "description": "Unique identifier for the convergence run that produced these findings."
    },
    "canonical_repo": {
      "type": "string",
      "description": "Name of the canonical (source) repository."
    },
    "derivative_repo": {
      "type": "string",
      "description": "Name of the derivative (target) repository."
    },
    "findings": {
      "type": "array",
      "items": { "$ref": "#/definitions/finding" },
      "description": "Array of individual convergence findings."
    },
    "summary": {
      "$ref": "#/definitions/summary",
      "description": "Aggregated summary of all findings in this report."
    }
  },
  "definitions": {
    "finding": {
      "type": "object",
      "required": [
        "id",
        "class",
        "severity",
        "source",
        "path",
        "details",
        "timestamp",
        "repo"
      ],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[A-Z_]+-[a-z_]+-[a-f0-9]+$",
          "description": "Unique finding identifier in the format SOURCE-CLASS-HASH."
        },
        "class": {
          "type": "string",
          "enum": [
            "missing_file",
            "extra_file",
            "content_drift",
            "stale_binding",
            "config_mismatch",
            "metadata_divergence",
            "schema_violation",
            "rule_gap",
            "rule_conflict",
            "phantom_rule",
            "scope_leak",
            "threshold_breach",
            "version_mismatch",
            "coverage_gap"
          ],
          "description": "Classification of the convergence finding."
        },
        "severity": {
          "type": "string",
          "enum": ["info", "warn", "deny"],
          "description": "Severity level of the finding."
        },
        "source": {
          "type": "string",
          "enum": [
            "scanner",
            "manifest_gen",
            "sync_validator",
            "policy_engine",
            "gate"
          ],
          "description": "Component that produced this finding."
        },
        "path": {
          "type": "string",
          "description": "File path relative to the repository root."
        },
        "details": {
          "type": "string",
          "description": "Human-readable description of the finding."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "ISO 8601 UTC timestamp when this finding was detected."
        },
        "repo": {
          "type": "string",
          "enum": ["ssid", "ssid-ems", "ssid-open-core"],
          "description": "Repository in which the finding was detected."
        },
        "evidence_hash": {
          "type": "string",
          "pattern": "^[a-f0-9]{64}$",
          "description": "SHA-256 hash of the relevant content for audit trail purposes."
        }
      }
    },
    "summary": {
      "type": "object",
      "required": [
        "total",
        "by_severity",
        "by_class",
        "by_source",
        "decision"
      ],
      "additionalProperties": false,
      "properties": {
        "total": {
          "type": "integer",
          "minimum": 0,
          "description": "Total number of findings in this report."
        },
        "by_severity": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "info": { "type": "integer", "minimum": 0 },
            "warn": { "type": "integer", "minimum": 0 },
            "deny": { "type": "integer", "minimum": 0 }
          },
          "description": "Finding counts grouped by severity level."
        },
        "by_class": {
          "type": "object",
          "propertyNames": {
            "enum": [
              "missing_file",
              "extra_file",
              "content_drift",
              "stale_binding",
              "config_mismatch",
              "metadata_divergence",
              "schema_violation",
              "rule_gap",
              "rule_conflict",
              "phantom_rule",
              "scope_leak",
              "threshold_breach",
              "version_mismatch",
              "coverage_gap"
            ]
          },
          "additionalProperties": {
            "type": "integer",
            "minimum": 0
          },
          "description": "Finding counts grouped by finding class."
        },
        "by_source": {
          "type": "object",
          "propertyNames": {
            "enum": [
              "scanner",
              "manifest_gen",
              "sync_validator",
              "policy_engine",
              "gate"
            ]
          },
          "additionalProperties": {
            "type": "integer",
            "minimum": 0
          },
          "description": "Finding counts grouped by source component."
        },
        "decision": {
          "type": "string",
          "enum": ["pass", "warn", "deny", "error"],
          "description": "Overall gate decision based on aggregated findings."
        }
      }
    }
  }
}

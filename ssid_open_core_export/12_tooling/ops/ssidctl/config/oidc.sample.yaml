# OIDC Configuration — DISABLED BY DEFAULT
# Schema: 16_codex/contracts/ssidctl/oidc.schema.json
#
# OIDC is optional. When enabled, requires a LOCAL jwks_path.
# No remote JWKS fetch. If enabled and jwks missing => FAIL hard.

enabled: false

# Uncomment and configure when needed:
# issuer: "https://accounts.example.com"
# client_id: "ssidctl-local"
# audience: "ssidctl"
# algorithms:
#   - RS256
# jwks_path: "/path/to/local/jwks.json"  # REQUIRED when enabled. Must exist on disk.
